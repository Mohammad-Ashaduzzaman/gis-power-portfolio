(function(){
 'use strict';
 const projects=window.PORTFOLIO_PROJECTS,grid=document.getElementById('projectGrid');
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 let filter='All',current=0,lastFocus=null;
 grid.innerHTML=projects.map(p=>`<article class="project-card" data-id="${p.id}" data-groups="${esc(p.groups.join('|'))}"><div class="project-visual"><a class="project-shot-link" href="projects/${p.id}.html" target="_blank" rel="noopener" aria-label="Open ${esc(p.title)} interactive Web GIS"><img class="project-shot" loading="lazy" width="800" height="480" src="assets/thumbnails/${p.id}.svg" alt="${esc(p.title)} — preview drawn from project geography"></a><span class="project-badge">Interactive case study</span><span class="project-code">${p.id}</span></div><div class="project-body"><small>${esc(p.primary)}</small><h3>${esc(p.title)}</h3><p>${esc(p.summary)}</p><div class="project-methods">${p.methods.slice(0,4).map(m=>`<span>${esc(m)}</span>`).join('')}</div><div class="project-actions"><button class="project-overview" data-overview="${p.id}" type="button">Project overview →</button><a class="project-launch" href="projects/${p.id}.html" target="_blank" rel="noopener">Launch Web GIS ↗</a></div></div></article>`).join('');
 const empty=document.createElement('div');empty.className='project-empty';empty.hidden=true;empty.innerHTML='<h3>No projects found</h3><p>Try another keyword or reset the category.</p><button class="btn secondary" id="resetProjects" type="button">Show all projects</button>';grid.append(empty);
 function applyFilters(){const q=document.getElementById('projectSearch').value.trim().toLowerCase();let count=0;projects.forEach(p=>{const yes=(filter==='All'||p.groups.includes(filter))&&[p.id,p.title,p.summary,p.primary,...p.methods].join(' ').toLowerCase().includes(q);grid.querySelector(`[data-id="${p.id}"]`).classList.toggle('hidden',!yes);if(yes)count++});document.getElementById('projectCount').textContent=count+' of '+projects.length+' projects';empty.hidden=!!count;document.querySelectorAll('#filters button').forEach(b=>{const active=b.dataset.filter===filter;b.classList.toggle('active',active);b.setAttribute('aria-pressed',String(active))})}
 document.querySelectorAll('#filters button').forEach(btn=>btn.addEventListener('click',()=>{filter=btn.dataset.filter;applyFilters()}));document.getElementById('projectSearch').addEventListener('input',applyFilters);document.getElementById('resetProjects').onclick=()=>{filter='All';document.getElementById('projectSearch').value='';applyFilters()};
 document.querySelectorAll('[data-project-view]').forEach(btn=>btn.addEventListener('click',()=>{grid.classList.toggle('list-view',btn.dataset.projectView==='list');document.querySelectorAll('[data-project-view]').forEach(b=>b.setAttribute('aria-pressed',String(b===btn)))}));
 const modal=document.getElementById('modalBg'),close=document.getElementById('modalClose');
 function openModal(p){if(!modal.classList.contains('open'))lastFocus=document.activeElement;current=projects.indexOf(p);document.getElementById('modalTag').textContent=p.id+' · Interactive case study';document.getElementById('modalCategory').textContent=p.primary;document.getElementById('modalTitle').textContent=p.title;document.getElementById('modalSummary').textContent=p.summary;document.getElementById('modalValue').textContent=p.value;document.getElementById('modalMethods').innerHTML=p.methods.map(m=>`<span class="method-chip">${esc(m)}</span>`).join('');document.getElementById('modalLaunch').href=`projects/${p.id}.html`;document.getElementById('modalImage').src=`assets/thumbnails/${p.id}.svg`;document.getElementById('modalImage').alt=p.title+' — project data preview';document.getElementById('modalMediaKicker').textContent='Spatial evidence · interactive delivery';document.getElementById('modalMediaTitle').textContent=p.id+' · '+p.primary;document.getElementById('modalPosition').textContent=(current+1)+' / '+projects.length;modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';document.querySelector('main').inert=true;document.querySelector('.header').inert=true;document.querySelector('footer').inert=true;close.focus({preventScroll:true})}
 function closeModal(){if(!modal.classList.contains('open'))return;modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow='';document.querySelector('main').inert=false;document.querySelector('.header').inert=false;document.querySelector('footer').inert=false;lastFocus?.focus({preventScroll:true})}
 grid.addEventListener('click',e=>{const btn=e.target.closest('[data-overview]');if(btn)openModal(projects.find(p=>p.id===btn.dataset.overview))});close.addEventListener('click',closeModal);document.getElementById('modalClose2').addEventListener('click',closeModal);modal.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.getElementById('modalPrevious').onclick=()=>openModal(projects[(current-1+projects.length)%projects.length]);document.getElementById('modalNext').onclick=()=>openModal(projects[(current+1)%projects.length]);
 document.addEventListener('keydown',e=>{if(!modal.classList.contains('open'))return;if(e.key==='Escape')closeModal();if(e.key==='ArrowRight')document.getElementById('modalNext').click();if(e.key==='ArrowLeft')document.getElementById('modalPrevious').click();if(e.key==='Tab'){const nodes=[...modal.querySelectorAll('a[href],button:not([disabled]),[tabindex="0"]')];if(e.shiftKey&&document.activeElement===nodes[0]){e.preventDefault();nodes.at(-1).focus()}else if(!e.shiftKey&&document.activeElement===nodes.at(-1)){e.preventDefault();nodes[0].focus()}}});
 const menu=document.getElementById('menu'),nav=document.getElementById('nav');menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(nav.classList.contains('open')))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.focus()}});
 document.getElementById('year').textContent=new Date().getFullYear();window.addEventListener('scroll',()=>{const h=document.documentElement;document.getElementById('progress').style.width=(h.scrollTop/Math.max(1,h.scrollHeight-h.clientHeight)*100)+'%'},{passive:true});
 if('IntersectionObserver' in window){const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced)document.body.classList.add('js-motion');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');observer.unobserve(e.target)}}),{threshold:.05});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const links=[...nav.querySelectorAll('a[href^="#"]')];const spy=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)links.forEach(a=>{const active=a.getAttribute('href')==='#'+e.target.id;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')})})},{rootMargin:'-20% 0px -60% 0px'});document.querySelectorAll('section[id]').forEach(el=>spy.observe(el))}
 document.getElementById('copyEmail').addEventListener('click',async e=>{const b=e.currentTarget;try{if(!navigator.clipboard?.writeText)throw new Error('Clipboard not available');await navigator.clipboard.writeText('plan.ashad@gmail.com');b.textContent='Email copied';setTimeout(()=>b.textContent='Copy email address',2500)}catch{b.textContent='plan.ashad@gmail.com';b.setAttribute('aria-label','Email address: plan dot ashad at gmail dot com')}});
 applyFilters();window.PORTFOLIO_APP={projects,openModal,closeModal,projectCount:projects.length};
})();
